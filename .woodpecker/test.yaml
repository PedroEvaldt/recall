when:
  - event: push

steps:
  - name: test
    image: golang:1.26.2-alpine
    commands:
      - go test -cover ./...
    volumes:
      - go-mod-cache:/go/pkg/mod
      - go-build-cache:/root/.cache/go-build
